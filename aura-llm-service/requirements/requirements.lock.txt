# ============================================================================
# Deploy lock — exact pins of the direct dependencies (M1).
#
# requirements*.txt hold floors (>=) for local development; this file pins the
# test-verified versions so container builds are deterministic and a transitive
# upgrade cannot silently change what ships to production.
#
# To regenerate after a verified upgrade, bump the version here to match the
# floor in the corresponding requirements*.txt. For a fully reproducible lock
# that also pins transitive dependencies with hashes, run in CI:
#     pip-compile --generate-hashes --output-file requirements/requirements.lock.txt \
#         requirements/requirements.txt requirements/requirements-observability.txt \
#         requirements/requirements-guardrails.txt
# ============================================================================

# ── Web framework & server ──────────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.49.0

# ── LangChain & AI ──────────────────────────────────────────────────────────
langchain-core==1.4.6
langgraph==1.2.4
langchain-ollama==1.1.0

# ── External services ───────────────────────────────────────────────────────
httpx==0.28.1
redis[asyncio]==8.0.0

# ── Utilities ───────────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
tenacity==9.1.4
aiobreaker==1.2.0

# ── Monitoring ──────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==8.0.0
python-json-logger==4.1.0

# ── Observability (optional; tracing) ───────────────────────────────────────
openinference-instrumentation-langchain==0.1.66
arize-phoenix-otel==0.16.1

# ── Guardrails (optional) ───────────────────────────────────────────────────
nemoguardrails==0.22.0
