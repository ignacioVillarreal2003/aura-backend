[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: requires Docker infra and RUN_INTEGRATION=1
    slow: long-running or optional pipeline tests (non-integration slow tests need RUN_SLOW=1)
filterwarnings =
    ignore::DeprecationWarning
