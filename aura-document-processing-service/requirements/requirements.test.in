# ============================================
# SOURCE of truth for test/dev dependencies (ranges).
#
# Compiled into the fully-pinned, hash-locked `requirements.test.txt` with:
#
#   make lock        (see scripts/compile_requirements.sh)
#
# Constrained against the compiled CPU lock so shared transitive packages
# resolve to the same versions the application is locked to (no install-time
# conflicts when both files are installed in the same environment).
# ============================================
-c requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
